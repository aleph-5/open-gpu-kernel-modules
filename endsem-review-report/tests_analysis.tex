\mainsection{Testing and Analysis}

The testing and analysis was done in two parts
\begin{itemize}
    \item \textbf{Correctness Testing}
    \item \textbf{Performance Testing}
\end{itemize}

\mainsubsection{Correctness Testing}
The correctness was evaluated using CUDA tests under \texttt{correctness\_tests}.
The suites are intended to cover the following categories :
\begin{enumerate}

    \item \textbf{\texttt{access\_type\_filtering}}
          \\Tests that only writes (including atomic read-modify-write operations) are
          recorded as dirty, while pure reads leave no trace.
          Also verifies that prefetched pages are properly tracked and that distinct
          access types to the same page are correctly de-duplicated.\\

    \item \textbf{\texttt{address\_range\_filtering}}
          \\Verifies that dirty tracking correctly isolates multiple non-contiguous
          allocations, handles concurrent multi-threaded writes to distinct sub-ranges,
          and produces page-aligned dump entries even when GPU accesses are at
          sub-page granularity.
          Also tests that inverted or zero-length range arguments do not suppress
          entries, and that pages whose PTEs were established before \texttt{start\_track}
          are still captured when written after tracking begins.\\

    \item \textbf{\texttt{backend\_correctness}}
          \\Validates fundamental invariants of the tracking system,
          all dump entries must be page-aligned with no duplicates, sparse write
          patterns must not produce phantom entries, and timestamps within and across
          epochs must be non-decreasing.
          Also verifies that freeing an allocation does not erase its tracking records,
          a page allocated after tracking is still tracked, 
          and that empty epochs between writes yield empty dumps.\\

    \item \textbf{\texttt{cumulative\_mode}}
          \\Confirms that cumulative mode accumulates pages across epochs (all pages
          from prior windows remain visible in subsequent dumps) and that
          re-writing an already-tracked page does not create a duplicate or update
          its timestamp (first-write-wins semantics hold across epochs).
          A stop/restart in delta mode is also verified to reset the tracking state
          completely.\\

    \item \textbf{\texttt{determinism}}
          \\Tests the cutover border: pages written before cutover must appear
          in the resulting snapshot while pages written after must not, even when the
          two write phases are interleaved closely in time.
          Also tests pause/resume semantics, verifying that writes issued during a
          pause are not recorded and that tracking resumes cleanly after
          \texttt{resume\_track}.\\

    \item \textbf{\texttt{first\_write\_wins}}
          \\Verifies the \textit{first write wins policy}, re-writing the same page must
          not update its timestamp or produce a duplicate entry.
          This is tested both for sequential re-writes and for 64 concurrent CUDA
          streams racing to write the same page simultaneously.
          Also confirms that after cutover (without PTE re-invalidation), a second
          write to the same page does not re-appear in the next epoch.\\

    \item \textbf{\texttt{footprint\_clearing}}
          \\Checks that stopping tracking fully restores page permissions:
          pages that were READ\_WRITE before tracking must be READ\_WRITE again,
          and pages that had no prior GPU mapping
          must not be spuriously granted READ\_WRITE on stop.
          Since there is no user space API to verify this this is done via write latency.
          Idempotency across multiple consecutive start/stop cycles is also verified.\\

    \item \textbf{\texttt{lifecycle\_races}}
          \\Stress-tests concurrent and destructive lifecycle transitions:
          stop while a GPU kernel is writing, rapid start/stop cycling,
          two processes attempting simultaneous tracking (second must get
          \texttt{EBUSY}), owner process death (next process must recover cleanly),
          \texttt{stop} while a dump is being read, \texttt{pause} with in-flight
          faults, and \texttt{resume} correctly re-downgrading PTEs.\\

    \item \textbf{\texttt{ordering}}
          \\Validates temporal ordering guarantees, writes before \texttt{start\_track}
          must not appear in the dump, timestamps must be monotonically
          non-decreasing, queries in cumulative mode must be non-destructive, and
          epoch isolation must hold.
          Also tests concurrent CUDA streams and high-volume sentinel/flood write
          patterns to verify timestamp ordering under load.\\

    \item \textbf{\texttt{procfs\_robustness}}
          \\Tests error handling on invalid procfs interactions: double \texttt{start},
          \texttt{stop} without \texttt{start}, stopping with a wrong PID,
          malformed input strings, double cutover without an intervening dump,
          cutover or dump without an active session and changing the hint during
          an active tracking session.\\
          All calls must return well-defined errors without crashing or corrupting
          driver state.\\

    \item \textbf{\texttt{scale}}
          \\Exercises the backing data structure at scale, 100K page single epoch
          writes, incremental growth across many cutover/dump cycles,
          small chunk procfs reads of large dumps, and writes
          near the maximum span of a bitmap based backend to catch off by one
          or capacity errors.\\

    \item \textbf{\texttt{snapshot\_isolation}}
          \\Verifies snapshot atomicity, pages written after cutover must not
          appear in the current snapshot, empty epochs must not carry over pages
          from the previous epoch, concurrent inserts during a dump read must not
          corrupt the snapshot, and a second read of an already drained delta mode
          dump must return zero entries.\\

    \item \textbf{\texttt{stats\_accuracy}}
          \\Validates the diagnostic stats interface, the insert counter must
          match the number of distinct pages written, counters must reset to zero
          on stop/start, lock-wait counters must be non-negative, and stats
          must be disabled (zeroed or unavailable) by default.\\

    \item \textbf{\texttt{table\_lifecycle}}
          \\Tests the dirty-tracking table through basic create/destroy cycles,
          verifying that reinitialisation clears all entries, that stop/start
          produces an empty table, that multiple reinit cycles are stable under
          stress, and that two independent allocations tracked in one session
          both appear correctly in the dump.\\

    \item \textbf{\texttt{tracking\_hints}}
          \\Tests the hint interface for selecting the data structure backend,
          verifies that on using either hint, the output is consistent with the
          expected one, and that switching hits bwtween sessions does not interfere
          with the tracking state.\\

    \item \textbf{\texttt{generic}}
          \\A broad integration suite covering the core procfs interface
          (\texttt{dirty\_tracking\_start/stop/query\_cutover/query\_dump}).
          Validates that writes are tracked, reads are not, cutover atomically
          snapshots the epoch, stop fully resets state, \texttt{cudaFree} does
          not remove entries, and cumulative vs.\ delta mode behave as specified.
\end{enumerate}

\paragraph{Observations}
The tests were run as root, and the current implementation passed every test
that is present in the repository. The aggregate result was
\textbf{74/74 passed}: 
\texttt{table\_lifecycle} 5/5,
\texttt{ordering} 8/8,
\texttt{determinism} 2/2,
\texttt{first\_write\_wins} 3/3,
\texttt{cummulative\_mode} 4/4,
\texttt{address\_range\_filtering} 5/5,
\texttt{access\_type\_filtering} 4/4,
\texttt{snapshot\_isolation} 4/4,
\texttt{backend\_correctness} 9/9,
\texttt{procfs\_robustness} 8/8,
\texttt{lifecycle\_races} 7/7,
\texttt{scale} 4/4,
\texttt{stats\_accuracy} 4/4,
\texttt{footprint\_clearing} 3/3,
\texttt{tracking\_hints} 3/3,
and the \texttt{generic} suite 1/1. The per-test
runners for the suites report runtimes between 1.95\,s and 3.05\,s.

\begin{center}
\footnotesize
\begin{longtable}{>{\raggedright\arraybackslash}p{2.0cm} >{\raggedright\arraybackslash}p{4.5cm} >{\raggedright\arraybackslash}p{6.2cm} >{\raggedright\arraybackslash}p{1.0cm} >{\raggedright\arraybackslash}p{1.1cm}}
\toprule
Suite & Test & Purpose & Result & Time \\
\midrule
\endfirsthead
\toprule
Suite & Test & Purpose & Result & Time \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{Continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

Table Lifecycle  & \texttt{tc01\_basic\_lifecycle} & Basic start→write→cutover→dump→stop cycle & PASS & 2.00s \\
Table Lifecycle  & \texttt{tc02\_reinit\_clears\_entries}   & Reinit clears all tracked entries & PASS & 1.99s \\
Table Lifecycle  & \texttt{tc03\_stop\_start\_empty\_table} & stop+start yields an empty table & PASS & 1.95s \\
Table Lifecycle  & \texttt{tc04\_multi\_reinit\_stress} & Multiple reinit cycles are stable under stress & PASS & 2.01s \\
Table Lifecycle  & \texttt{tc05\_two\_allocs\_one\_table} & Two allocations both appear in one session's dump & PASS & 1.95s \\
\midrule

Ordering & \texttt{tc01\_write\_before\_start} & Pre-start writes do not appear in any dump & PASS & 2.00s \\
Ordering & \texttt{tc02\_ts\_monotonic} & Timestamps are monotonically non-decreasing & PASS & 2.01s \\
Ordering & \texttt{tc03\_query\_nondestructive} & Cumulative dump reads are non-destructive & PASS & 2.00s \\
Ordering & \texttt{tc04\_empty\_table\_active} & Active session with no writes returns empty dump & PASS & 2.00s \\
Ordering & \texttt{tc05\_epoch\_isolation} & Each epoch's dump contains only that epoch's writes & PASS & 2.02s \\
Ordering & \texttt{tc06\_concurrent\_streams} & Timestamps ordered correctly across concurrent streams & PASS & 1.99s \\
Ordering & \texttt{tc07\_sentinel\_flood} & Sentinel page timestamp precedes 1M-thread flood & PASS & 1.95s \\
Ordering & \texttt{tc08\_flood\_between\_queries} & Phase-A max timestamp $\leq$ phase-B min timestamp & PASS & 1.98s \\
\midrule

Determinism & \texttt{tc01\_cutover\_dump\_border} & Pre-cutover pages in snap1; post-cutover pages in snap2 & PASS & 1.99s \\
Determinism & \texttt{tc02\_pause\_resume\_semantics} & Paused writes not recorded; resume re-arms tracking & PASS & 2.02s \\
\midrule

First Write Wins & \texttt{tc01\_same\_page\_repeat\_write} & Re-writing a page preserves original timestamp & PASS & 1.99s \\
First Write Wins & \texttt{tc02\_concurrent\_same\_page} & 64 streams racing on one page produce exactly 1 entry & PASS & 2.00s \\
First Write Wins & \texttt{tc03\_redirty\_after\_cutover} & Post-cutover re-write does not appear in next epoch & PASS & 2.02s \\
\midrule

Cumulative Mode & \texttt{tc01\_cumulative\_basic} & Epoch 2 dump contains all pages from both epochs & PASS & 2.03s \\
Cumulative Mode & \texttt{tc02\_cumul\_vs\_delta\_consist.} & Each delta dump is a subset of the cumulative dump & PASS & 2.44s \\
Cumulative Mode & \texttt{tc03\_cumul\_redirty\_idempotent} & Re-writing a tracked page produces no duplicate & PASS & 2.10s \\
Cumulative Mode & \texttt{tc04\_cumul\_stop\_start\_accum.} & Stop+restart resets state completely in delta mode & PASS & 2.00s \\
\midrule

Addr.\ Range Filtering & \texttt{tc01\_two\_alloc\_range\_isolation} & Two allocations are isolated in a single session & PASS & 1.99s \\
Addr.\ Range Filtering & \texttt{tc02\_range\_seq\_reads\_threaded} & Concurrent threaded writes to distinct sub-ranges & PASS & 2.03s \\
Addr.\ Range Filtering & \texttt{tc03\_subpage\_start\_rounding} & Sub-page accesses produce page-aligned dump entries & PASS & 2.05s \\
Addr.\ Range Filtering & \texttt{tc04\_inverted\_and\_zero\_range} & Inverted/zero-length range does not suppress entries & PASS & 2.06s \\
Addr.\ Range Filtering & \texttt{tc05\_filter\_before\_start\_track} & Pages with pre-start PTEs still tracked when written & PASS & 2.07s \\
\midrule

Access Type Filtering & \texttt{tc01\_read\_only\_no\_dirty} & Read-only GPU access produces no dirty entries & PASS & 2.05s \\
Access Type Filtering & \texttt{tc02\_atomic\_does\_dirty} & Atomic RMW records the page as dirty & PASS & 2.00s \\
Access Type Filtering & \texttt{tc03\_read\_then\_write\_distinct} & Read half leaves no trace; write half is tracked & PASS & 2.00s \\
Access Type Filtering & \texttt{tc04\_prefetch\_no\_dirty} & Prefetched pages are not recorded as dirty & PASS & 2.01s \\
\midrule

Snapshot Isolation & \texttt{tc01\_post\_cutover\_writes\_excl.} & Post-cutover writes excluded from current snapshot & PASS & 2.01s \\
Snapshot Isolation & \texttt{tc02\_empty\_epoch\_no\_carryover} & Empty epoch yields no carryover from previous epoch & PASS & 2.07s \\
Snapshot Isolation & \texttt{tc03\_conc.\_insert\_during\_dump} & Concurrent inserts do not corrupt snapshot being read & PASS & 2.17s \\
Snapshot Isolation & \texttt{tc04\_multiple\_readers\_same\_snap.} & Second read of drained delta dump returns 0 entries & PASS & 2.02s \\
\midrule

Backend Correctness & \texttt{tc01\_all\_entries\_page\_aligned} & All dump entries are page-aligned, no duplicates & PASS & 2.03s \\
Backend Correctness & \texttt{tc02\_sparse\_write\_pattern} & Sparse writes produce no phantom entries & PASS & 2.01s \\
Backend Correctness & \texttt{tc03\_address\_monotonic\_in\_dump} & Reports whether dump output is address-monotonic & PASS & 1.96s \\
Backend Correctness & \texttt{tc04\_multiple\_allocs\_independent} & Multiple allocations tracked independently, no aliasing & PASS & 2.03s \\
Backend Correctness & \texttt{tc05\_invalidate\_doesnt\_clear} & \texttt{cudaFree} does not remove tracking entries & PASS & 2.01s \\
Backend Correctness & \texttt{tc06\_ts\_monotonic\_within\_epoch} & Timestamps non-decreasing within a single epoch & PASS & 2.00s \\
Backend Correctness & \texttt{tc07\_cross\_epoch\_ts\_ordering} & Max ts of epoch $N \leq$ min ts of epoch $N\!+\!1$ & PASS & 2.01s \\
Backend Correctness & \texttt{tc08\_empty\_epoch\_between\_writes} & Empty epoch between writes yields empty dump & PASS & 2.02s \\
Backend Correctness & \texttt{tc09\_alloc\_after\_start\_tracked} & Post-start allocation captured when written & PASS & 2.04s \\
\midrule

Procfs Robustness & \texttt{tc01\_double\_start} & Second \texttt{start} returns EBUSY; state intact & PASS & 2.03s \\
Procfs Robustness & \texttt{tc02\_stop\_without\_start} & \texttt{stop} without prior start returns error & PASS & 2.00s \\
Procfs Robustness & \texttt{tc03\_wrong\_pid\_stop} & Wrong-PID stop returns error; own session unaffected & PASS & 1.97s \\
Procfs Robustness & \texttt{tc04\_malformed\_start\_write} & Malformed start strings return EINVAL & PASS & 1.99s \\
Procfs Robustness & \texttt{tc05\_double\_cutover\_no\_dump} & Second cutover returns EBUSY; snapshot preserved & PASS & 1.99s \\
Procfs Robustness & \texttt{tc06\_cutover\_without\_start} & Cutover/dump without active session return errors & PASS & 2.01s \\
Procfs Robustness & \texttt{tc07\_hint\_invalid\_input} & Invalid hint input returns EINVAL & PASS & 2.01s \\
Procfs Robustness & \texttt{tc08\_hint\_busy\_during\_tracking} & Trying to write to \texttt{dirty\_tracking\_active} during tracking returns EBUSY & PASS & 2.03s \\
\midrule

Lifecycle Races & \texttt{tc01\_stop\_while\_kernel\_running} & \texttt{stop} during active GPU write does not crash & PASS & 1.99s \\
Lifecycle Races & \texttt{tc02\_rapid\_start\_stop\_cycle} & Rapid start/stop cycles leave no leaks or corruption & PASS & 2.00s \\
Lifecycle Races & \texttt{tc03\_conc.\_start\_two\_processes} & Second process attempting start receives EBUSY & PASS & 1.95s \\
Lifecycle Races & \texttt{tc04\_owner\_process\_dies} & New session works after owner process is killed & PASS & 2.04s \\
Lifecycle Races & \texttt{tc05\_stop\_while\_dump\_reading} & \texttt{stop} during slow dump read does not deadlock & PASS & 3.05s \\
Lifecycle Races & \texttt{tc06\_pause\_with\_inflight\_faults} & Pause stops recording; resume re-enables it & PASS & 1.99s \\
Lifecycle Races & \texttt{tc07\_resume\_re\_downgrade} & Resume re-downgrades PTEs so writes are re-tracked & PASS & 2.01s \\
\midrule

Scale & \texttt{tc01\_large\_page\_count} & 100K pages tracked in one epoch without truncation & PASS & 2.29s \\
Scale & \texttt{tc02\_repeated\_growth\_and\_query} & Incremental growth over many cutover/dump cycles & PASS & 2.04s \\
Scale & \texttt{tc03\_procfs\_paginated\_read} & Small-chunk dump reads produce correct output & PASS & 2.01s \\
Scale & \texttt{tc04\_near\_bitmap\_max} & Full-span write near bitmap backend capacity limit & PASS & 2.00s \\
\midrule

Stats Accuracy & \texttt{tc01\_insert\_count\_matches\_pages} & Insert counter equals number of distinct pages written & PASS & 2.02s \\
Stats Accuracy & \texttt{tc02\_stats\_reset\_on\_reinit} & Stats counters reset to zero on stop+start & PASS & 2.01s \\
Stats Accuracy & \texttt{tc03\_lock\_wait\_nonneg} & Lock-wait counter is non-negative after a session & PASS & 1.96s \\
Stats Accuracy & \texttt{tc04\_stats\_disabled\_by\_default} & Stats disabled by default; toggle required & PASS & 1.97s \\
\midrule

Footprint Clearing & \texttt{tc01\_rw\_restored\_after\_stop} & Pre-tracking RW pages restored to RW after stop & PASS & 1.99s \\
Footprint Clearing & \texttt{tc02\_fresh\_pages\_still\_fault} & Unmapped pages not spuriously granted RW on stop & PASS & 2.02s \\
Footprint Clearing & \texttt{tc03\_idempotent\_cycles} & Multiple start/stop cycles each leave pages in RW state & PASS & 1.95s \\
\midrule

Tracking Hints & \texttt{tc01\_hint\_seq\_session} & Giving hint as \texttt{WRITE\_SEQ} all the writes are recorded & PASS & 2.00s \\
Tracking Hints & \texttt{tc02\_hint\_rand\_session} & Giving hint as \texttt{WRITE\_RAND} all the writes are recorded & PASS & 1.98s \\
Tracking Hints & \texttt{tc03\_hint\_switch\_between\_sessions} & Switching hints in between tracking sessions does not raise any errors & PASS & 1.97s \\
\midrule

Generic & \texttt{test\_dirty\_tracking\_suite} & Integration smoke test covering all core behaviors & PASS & 2.02s \\

\end{longtable}
\normalsize
\end{center}

\mainsubsection{Performance Testing}
The performance overhead of the dirty-tracking pipeline was evaluated using the
\texttt{run\_overhead\_benchmark.sh} script, which measures wall-clock time for
each benchmark with tracking \textbf{OFF} (baseline) and tracking \textbf{ON},
then reports average wall time, standard deviation, and overhead percentage
in a CSV.
Each benchmark is repeated \texttt{REPS} times (default: 5) and the
results are averaged.
Seven benchmarks spanning write-intensive, compute-bound, read-heavy, and
mixed access patterns were selected:
\texttt{int\_set\_uvm} (4K-stride and sequential),
polybench GEMM, 2MM, BICG, and MVT,
and Rodinia Needle (Needleman--Wunsch).

\paragraph{Memory Configurations}
Each benchmark is run across eight working-set sizes that span
both the \emph{in-GPU} regime and the \emph{oversubscribed} regime:

\begin{itemize}
    \item \textbf{In-GPU} (fits in VRAM of the test A40, $\approx$45\,GB):
          512\,MB, 4\,GB, 8\,GB, 16\,GB, 32\,GB, 40\,GB.
    \item \textbf{Oversubscribed} (exceeds GPU VRAM, triggers UVM eviction to host):
          48\,GB, 64\,GB.
\end{itemize}


\paragraph{Benchmarks}
\begin{enumerate}

    \item \textbf{\texttt{int\_set\_uvm} (4K stride)}
          \\Worst-case write-intensive synthetic: every write touches a fresh page at
          4\,KB stride, maximising UVM fault events and dirty-tracking insertions per
          byte written.
          Run across both in-GPU and oversubscribed configurations to measure how
          overhead scales with working-set size and to observe eviction-driven
          re-faulting behavior.

    \item \textbf{\texttt{int\_set\_uvm} (sequential)}
          \\Control for the 4K-stride variant: coalesced sequential writes to the same
          allocation generate fewer fault events per byte, isolating per-fault tracking
          overhead from per-byte overhead.
          Compared directly with the stride variant to quantify the impact of access
          pattern on tracking cost across identical working-set sizes.

    \item \textbf{polybench GEMM}
          \\Compute-bound reference: high arithmetic density amortises tracking cost
          over long kernel runtimes, measuring overhead on kernels dominated by
          floating-point computation rather than memory faults.
          Also run in the oversubscribed regime; a parameter-driven regime shift at
          large matrix sizes causes baseline time to drop sharply, making the fixed
          tracking overhead appear proportionally larger.

    \item \textbf{polybench 2MM}
          \\Chained dual-GEMM benchmark: two consecutive matrix multiplications produce
          a larger write footprint across two output matrices, testing tracking-table
          scalability under heavier write load than a single GEMM while retaining high
          arithmetic intensity.
          Oversubscribed configurations were not collected for this benchmark.

    \item \textbf{polybench BICG}
          \\Read-heavy workload: predominantly read traffic exercises the tracking
          pipeline's read-fault bypass path and measures overhead on kernels with a
          minimal write footprint.
          Also run in the oversubscribed regime, where the fraction of time spent in
          fault handling increases and amplifies per-insertion cost.

    \item \textbf{polybench MVT}
          \\Read-mostly with a small write component: verifies that a minor write
          footprint is correctly tracked without inflating overhead on otherwise
          read-heavy traffic.
          Complements BICG with a slightly different read-to-write ratio and is
          also exercised in the oversubscribed regime.

    \item \textbf{Rodinia Needle (Needleman--Wunsch)}
          \\Real-world dynamic-programming kernel with mixed read/write access to a
          2-D score matrix; working-set size is controlled via the \texttt{-mb}
          parameter.
          Above a working-set threshold the benchmark reduces its iteration count,
          drastically shrinking the write footprint and the number of UVM faults
          fired---causing overhead to collapse from $>$800\% at small sizes to
          near zero at large ones.

\end{enumerate}

\paragraph{Results}
Table~\ref{tab:overhead} reports measured overhead percentages for all
benchmark/configuration pairs (5 repetitions each, averaged).
The shaded rows mark the two oversubscribed configurations (48\,GB and 64\,GB)
where the working set exceeds GPU VRAM and UVM must evict pages to host memory.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{%
  >{\raggedright\arraybackslash}p{1.3cm}
  >{\centering\arraybackslash}p{1.45cm}
  >{\centering\arraybackslash}p{1.3cm}
  >{\centering\arraybackslash}p{1.05cm}
  >{\centering\arraybackslash}p{1.05cm}
  >{\centering\arraybackslash}p{1.05cm}
  >{\centering\arraybackslash}p{1.05cm}
  >{\centering\arraybackslash}p{1.3cm}
}
\toprule
\textbf{Size} &
\textbf{4K stride} &
\textbf{Sequential} &
\textbf{GEMM} &
\textbf{2MM} &
\textbf{BICG} &
\textbf{MVT} &
\textbf{Needle} \\
\midrule
512\,MB  & $+57.5\%$   & $+15.7\%$  & $+26.0\%$  & $+21.5\%$ & $+26.9\%$  & $+31.7\%$  & $+113.9\%$ \\
4\,GB    & $+372.3\%$  & $+163.4\%$ & $+19.3\%$  & $+67.2\%$ & $+131.1\%$ & $+131.7\%$ & $+871.0\%$ \\
8\,GB    & $+659.9\%$  & $+302.9\%$ & $+15.1\%$  & $+39.6\%$ & $+171.3\%$ & $+153.1\%$ & $+959.8\%$ \\
16\,GB   & $+1108.2\%$ & $+457.9\%$ & $+13.0\%$  & $+11.2\%$ & $+191.0\%$ & $+178.4\%$ & $+871.5\%$ \\
32\,GB   & $+1537.6\%$ & $+630.0\%$ & $+207.6\%$ & $+9.8\%$  & $+257.9\%$ & $+265.2\%$ & $+0.6\%$   \\
40\,GB   & $+1688.3\%$ & $+703.2\%$ & $+258.8\%$ & $+9.1\%$  & $+264.6\%$ & $+257.0\%$ & $+19.1\%$  \\
\rowcolor[gray]{0.92}
48\,GB$^\dagger$ & $+1730.7\%$ & $+699.4\%$ & $+213.1\%$ & ---  & $+263.6\%$ & $+290.6\%$ & $+27.8\%$  \\
\rowcolor[gray]{0.92}
64\,GB$^\dagger$ & $+1547.1\%$ & $+648.6\%$ & $+261.6\%$ & ---  & $+208.4\%$ & $+253.5\%$ & $+11.8\%$  \\
\bottomrule
\end{tabular}
\caption{Dirty-tracking overhead percentage by benchmark and working-set size
         (tracking ON vs.\ OFF wall time, averaged over 5 runs).
         Column headers \emph{4K stride} and \emph{Sequential} refer to the two
         \texttt{int\_set\_uvm} variants.
         $^\dagger$~Oversubscribed. 2MM oversubscribed runs were not collected.}
\label{tab:overhead}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{overhead_write.pdf}
\caption{Overhead for write-intensive \texttt{int\_set\_uvm} benchmarks.
         The 4K-stride variant maximises UVM fault events per byte written,
         producing the highest overhead of any benchmark tested.
         Sequential access generates fewer faults per byte and
         achieves 2-8$\times$ lower overhead at comparable sizes.
         Grey shaded region: oversubscribed regime ($\geq$48\,GB).}
\label{fig:overhead_write}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{overhead_polybench.pdf}
\caption{Overhead for Polybench kernels.
         GEMM and 2MM show low overhead while the working set fits in VRAM,
         as compute time dominates fault-handling cost.
         BICG and MVT are read-heavy but still accumulate non-trivial overhead
         from their write footprints and initial page-population faults.
         Grey shaded region: oversubscribed regime.}
\label{fig:overhead_polybench}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{overhead_needle.pdf}
\caption{Rodinia Needle (Needleman-Wunsch).
         \emph{Left:} wall-time comparison (tracking OFF vs.\ ON) with error bars
         ($\pm$1 standard deviation over 5 runs).
         \emph{Right:} derived overhead percentage.
         Overhead is very high at 4-16\,GB (871-960\%) and collapses near
         zero at $\geq$32\,GB due to an algorithmic regime shift in the
         benchmark at that working-set size.}
\label{fig:overhead_needle}
\end{figure}

\paragraph{Analysis of Results}
\mbox{}\\
\textbf{Write-intensive workloads.}\enspace
The \texttt{int\_set\_uvm} benchmarks represent the worst case for the tracking
pipeline: every write touches a fresh page, so the number of dirty-tracking
insertions equals the total page count.
With 4K-stride access, overhead scales from $+57.5\%$ at 512\,MB to $+1730.7\%$
at 48\,GB because a larger working set generates proportionally more UVM fault
events, each carrying a tracking insertion.
Sequential access produces the same number of dirty pages but consolidates
memory traffic so that fewer fault events fire per byte written, reducing
overhead by 2-8$\times$ at comparable sizes (Figure~\ref{fig:overhead_write}).
In the oversubscribed regime ($\geq$48\,GB) pages are repeatedly evicted and
re-faulted, but first-write-wins semantics within each epoch cap insertions to one
per page regardless of eviction frequency.
The slight drop in 4K-stride overhead from 48\,GB ($+1730.7\%$) to 64\,GB
($+1547.1\%$) reflects that at 64\,GB baseline execution time grows faster than
tracking overhead, as the eviction subsystem dominates baseline performance.

\textbf{Compute-bound workloads (GEMM, 2MM).}\enspace
When the working set fits in VRAM, arithmetic intensity dominates and tracking
adds only 13-26\% for GEMM.
At $\geq$32\,GB the benchmark's matrix-dimension parameter changes and the
baseline execution time drops sharply (205\,s at 16\,GB vs.\ 19\,s at 32\,GB),
making the fixed tracking overhead appear proportionally larger (207-261\%).
2MM achieves the lowest overhead of any benchmark at large in-GPU sizes
(9.1-9.8\% at 32--40\,GB): two chained GEMM kernels increase total compute
time while the write footprint grows only modestly.

\textbf{Read-heavy workloads (BICG, MVT).}\enspace
Although most memory traffic is reads, both kernels show non-trivial overhead
(26-31\% at 512\,MB, up to $+290.6\%$ in the oversubscribed regime).
Read faults do not insert dirty records, but the initial page-population phase
and the small write component still generate write faults.
In the oversubscribed regime the fraction of time spent in fault handling
increases, amplifying the per-insertion cost (Figure~\ref{fig:overhead_polybench}).

\textbf{Rodinia Needle.}\enspace
Needle exhibits a two-regime pattern (Figure~\ref{fig:overhead_needle}).
At 512\,MB-16\,GB, overhead is high (113-960\%): the DP score matrix generates
dense write traffic, producing many first-write faults per kernel invocation.
At $\geq$32\,GB, both baseline and tracking-on execution times collapse from
$O(10)$\,s to $O(0.1)$\,s and overhead falls to near zero ($+0.6$-$+27.8\%$).
This transition is caused by the benchmark's \texttt{-mb} parameter: above a
working-set threshold the implementation reduces iteration count, drastically
shrinking the write footprint and the number of UVM faults fired.
The tracking system handles this correctly: near-zero work produces near-zero overhead.

\textbf{In-GPU vs.\ oversubscribed summary.}\enspace
Across all benchmarks, the in-GPU regime shows overhead primarily proportional
to write-fault density.
Compute-bound kernels that amortise faults over long runtimes (GEMM at 16\,GB,
2MM at 32-40\,GB) achieve single-digit to low two-digit percent overhead,
confirming the pipeline is lightweight relative to GPU computation.
The oversubscribed regime adds eviction-driven re-faulting, but first-write-wins
semantics cap dirty insertions to one per page per epoch regardless of eviction
frequency, preventing unbounded overhead growth.

\paragraph{Cross-Backend Overhead Comparison}
The seven available backends differ substantially in algorithmic complexity for
insert, lookup, and iteration.
Tables~\ref{tab:backend_overhead_stride} and~\ref{tab:backend_overhead_gemm}
compare wall-clock overhead across all backends for the two most discriminating
benchmarks: \texttt{int\_set\_uvm} 4K-stride (maximum insert pressure) and
polybench GEMM (compute-dominated, low insert pressure).
Each cell is the overhead percentage (tracking ON vs.\ OFF, 5-run average)
after recompiling the kernel module with the corresponding \texttt{.ops} pointer
set in \texttt{uvm\_common.c}.

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{%
  >{\raggedright\arraybackslash}p{1.1cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
}
\toprule
\textbf{Size} &
\rotatebox{90}{\textbf{Bitmap}} &
\rotatebox{90}{\textbf{XArray}} &
\rotatebox{90}{\textbf{Vector}} &
\rotatebox{90}{\textbf{Vec.-U}} &
\rotatebox{90}{\textbf{Chunked}} &
\rotatebox{90}{\textbf{Nested BM}} &
\rotatebox{90}{\textbf{Linked List}} \\
\midrule
512\,MB              & $+58.1\%$   & $+36.6\%$   & $+57.5\%$   & $+900.8\%$   & --- & --- & --- \\
4\,GB                & $+367.2\%$  & $+272.0\%$  & $+372.3\%$  & $+27730.1\%$ & --- & --- & --- \\
8\,GB                & $+615.5\%$  & $+462.1\%$  & $+659.9\%$  & DNF          & --- & --- & --- \\
16\,GB               & $+1076.5\%$ & $+680.8\%$  & $+1108.2\%$ & DNF          & --- & --- & --- \\
32\,GB               & $+1618.9\%$ & $+981.1\%$  & $+1537.6\%$ & DNF          & --- & --- & --- \\
40\,GB               & $+1687.0\%$ & $+1046.0\%$ & $+1688.3\%$ & DNF          & --- & --- & --- \\
\rowcolor[gray]{0.92}
48\,GB$^\dagger$     & $+1677.6\%$ & $+1038.7\%$ & $+1730.7\%$ & DNF          & --- & --- & --- \\
\rowcolor[gray]{0.92}
64\,GB$^\dagger$     & $+1611.5\%$ & $+942.0\%$  & $+1547.1\%$ & DNF          & --- & --- & --- \\
\bottomrule
\end{tabular}
\caption{\texttt{int\_set\_uvm} 4K-stride overhead by backend (5-run average,
         tracking ON vs.\ OFF).
         Vec.-U\,=\,Vector Unsorted; Nested BM\,=\,Nested Bitmap.
         Vector column carried from Table~\ref{tab:overhead}.
         $^\dagger$~Oversubscribed.}
\label{tab:backend_overhead_stride}
\end{table}

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{%
  >{\raggedright\arraybackslash}p{1.1cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
  >{\centering\arraybackslash}p{1.35cm}
}
\toprule
\textbf{Size} &
\rotatebox{90}{\textbf{Bitmap}} &
\rotatebox{90}{\textbf{XArray}} &
\rotatebox{90}{\textbf{Vector}} &
\rotatebox{90}{\textbf{Vec.-U}} &
\rotatebox{90}{\textbf{Chunked}} &
\rotatebox{90}{\textbf{Nested BM}} &
\rotatebox{90}{\textbf{Linked List}} \\
\midrule
512\,MB              & --- & $+26.3\%$  & $+26.0\%$  & $-93.3\%$  & --- & --- & --- \\
4\,GB                & --- & $+24.7\%$  & $+19.3\%$  & $+380.8\%$ & --- & --- & --- \\
8\,GB                & --- & $+17.1\%$  & $+15.1\%$  & $+551.3\%$ & --- & --- & --- \\
16\,GB               & --- & $+13.6\%$  & $+13.0\%$  & $+395.8\%$ & --- & --- & --- \\
32\,GB               & --- & $+266.8\%$ & $+207.6\%$ & $+224.1\%$ & --- & --- & --- \\
40\,GB               & --- & $+256.1\%$ & $+258.8\%$ & $+211.8\%$ & --- & --- & --- \\
\rowcolor[gray]{0.92}
48\,GB$^\dagger$     & --- & $+259.3\%$ & $+213.1\%$ & $+250.3\%$ & --- & --- & --- \\
\rowcolor[gray]{0.92}
64\,GB$^\dagger$     & --- & $+266.2\%$ & $+261.6\%$ & $+316.4\%$ & --- & --- & --- \\
\bottomrule
\end{tabular}
\caption{Polybench GEMM overhead by backend (5-run average, tracking ON vs.\ OFF).
         Vec.-U\,=\,Vector Unsorted; Nested BM\,=\,Nested Bitmap.
         Vector column carried from Table~\ref{tab:overhead}.
         $^\dagger$~Oversubscribed.}
\label{tab:backend_overhead_gemm}
\end{table}

\paragraph{Per-Operation Latency Across Backends}
The stats subsystem records per-call nanosecond timing for every backend
operation via \texttt{uvm\_dirty\_ds\_stats}.
Enable it by writing \texttt{enable} to the stats procfs node before the
benchmark run; results are written to \texttt{/tmp/uvm\_dirty\_ds\_stats.txt}.
Table~\ref{tab:backend_ops} reports average insert, lookup, and
\texttt{for\_each\_in\_range} latencies plus lock-wait time for each backend
measured under the \texttt{int\_set\_uvm} 4K-stride workload at 16\,GB
(the size where insert pressure is highest without entering the oversubscribed
regime).

\begin{table}[H]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{%
  >{\raggedright\arraybackslash}p{2.0cm}
  >{\centering\arraybackslash}p{1.5cm}
  >{\centering\arraybackslash}p{1.7cm}
  >{\centering\arraybackslash}p{1.5cm}
  >{\centering\arraybackslash}p{1.7cm}
  >{\centering\arraybackslash}p{1.7cm}
}
\toprule
\textbf{Backend} &
\textbf{Insert} & \textbf{Insert} &
\textbf{Lookup} & \textbf{Lookup} &
\textbf{For-each} \\
&
\textbf{avg (ns)} & \textbf{lock-wait (ns)} &
\textbf{avg (ns)} & \textbf{lock-wait (ns)} &
\textbf{avg (ns)} \\
\midrule
Bitmap        & --- & --- & --- & --- & --- \\
XArray        & --- & --- & --- & --- & --- \\
Vector        & --- & --- & --- & --- & --- \\
Vec. Unsorted & --- & --- & --- & --- & --- \\
Chunked       & --- & --- & --- & --- & --- \\
Nested Bitmap & --- & --- & --- & --- & --- \\
Linked List   & --- & --- & --- & --- & --- \\
\bottomrule
\end{tabular}
\caption{Average per-operation latency and lock-wait time per backend,
         measured via \texttt{uvm\_dirty\_ds\_stats} under
         \texttt{int\_set\_uvm} 4K-stride at 16\,GB.}
\label{tab:backend_ops}
\end{table}



